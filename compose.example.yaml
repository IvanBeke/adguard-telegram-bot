services:
  adguard-telegram-bot:
    build: .
    container_name: adguard-telegram-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=Europe/Madrid
    volumes:
      - ./data:/app/data